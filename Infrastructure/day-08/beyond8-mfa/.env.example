APP_NAME=your_app_name
API_PREFIX=/api
EXPOSE_API_DOCS=false
# Chỉ bạn mở được Swagger: đặt cả hai; không cần bật EXPOSE_API_DOCS công khai.
# DOCS_BASIC_AUTH_USER=you
# DOCS_BASIC_AUTH_PASSWORD=strong-random-secret

# Postgres connection string (sslmode=require when connecting to managed cloud DB)
DATABASE_URL=postgresql+psycopg2://user:password@host:5432/database
# Optional; async SQLAlchemy (e.g. postgresql+asyncpg://...)
# ASYNC_DATABASE_URL=

# JWT dùng cho auth-token và beyond-course-access token
JWT_SECRET_KEY=your_jwt_secret_key
JWT_ALGORITHM=HS256
COURSE_ACCESS_TOKEN_EXPIRE_DAYS=30

# OTP settings
# Redis key namespace for all cache keys
KEY_PREFIX=xxx
OTP_TTL_SECONDS=60
OTP_REFRESH_COOLDOWN_SECONDS=60

# Admin account is seeded automatically as admin@gmail.com

# CORS: optional extra origins (comma-separated), merged with built-in defaults in code
# (localhost:3000, mfa.beyond8.io.vn, source, hoctot, …) so this can be left empty.
# CORS_ORIGINS=https://preview-xxx.vercel.app

REDIS_ENABLED=true
# Prefer local / Docker Redis (TCP). When set, Upstash vars are ignored.
REDIS_URL=redis://127.0.0.1:6379/0
# Optional: Upstash REST (legacy) when REDIS_URL is empty
# UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url
# UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token