# Full stack (Postgres + Redis + API + Next): use ../docker-compose.yml from this folder.
services:
  api:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env
    ports:
      - "8000:8000"
