---
description: Update UI/UX for a target screen or component
---

Implement the requested UI update:
$ARGUMENTS

UI update requirements:
- Preserve existing business logic unless a behavior change is requested.
- Follow existing design system, spacing, typography, and color tokens.
- Keep responsive behavior and accessibility in mind (labels, focus, contrast).
- Minimize visual regressions and avoid unrelated refactors.
- Return: changed files, UI decisions, and quick verification steps.
